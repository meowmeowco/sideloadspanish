// Sideload Spanish — Tooltip (stub)
// Will handle hover/click interactions on replaced words.
// Implemented in Phase 2.

(() => {
  // Placeholder — tooltip.js is loaded by manifest but has no behavior yet.
})();
